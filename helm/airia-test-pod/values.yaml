# ===============================================================================
# AIRIA TEST POD - DEFAULT CONFIGURATION
# ===============================================================================
# This Helm chart deploys an infrastructure validation tool that tests various
# services and configurations in your Kubernetes environment.
#
# Each test appears as a card in the web UI dashboard at http://your-ingress-url/
# Tests can be run individually or all at once to validate infrastructure readiness.
# ===============================================================================

# Default values for airia-test-pod
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

# ===============================================================================
# DEPLOYMENT CONFIGURATION
# ===============================================================================

replicaCount: 1

image:
  repository: ghcr.io/davidpacold/airia-test-pod
  pullPolicy: Always
  # tag defaults to .Chart.AppVersion if not specified
  # The chart automatically prepends 'v' prefix (e.g., v1.0.170)

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

# ===============================================================================
# KUBERNETES RESOURCES
# ===============================================================================
# Configure Kubernetes-specific settings for the test pod deployment.
# These settings control security, networking, and resource management.
# ===============================================================================

# ---------------------------------------------------------------------------
# SERVICE ACCOUNT
# ---------------------------------------------------------------------------
# Service accounts provide an identity for processes running in pods.
# The test pod needs a service account to:
# - Create/delete PVCs for storage testing
# - Access Kubernetes API for cluster information
# - Run tests that validate Kubernetes functionality
serviceAccount:
  create: true  # Automatically create a service account for the pod
  annotations: {}  # Add cloud provider annotations if needed (e.g., AWS IAM roles)
  # Example for AWS EKS with IAM roles for service accounts (IRSA):
  # annotations:
  #   eks.amazonaws.com/role-arn: arn:aws:iam::123456789012:role/airia-test-pod
  name: ""  # Leave empty to auto-generate name based on release name

# Advanced Kubernetes settings (optional)
podAnnotations: {}  # Add custom annotations for monitoring/service mesh

# Pod-level security settings
# These settings enhance security by enforcing non-root execution and seccomp
podSecurityContext:
  runAsNonRoot: true
  runAsUser: 1000
  fsGroup: 1000
  seccompProfile:
    type: RuntimeDefault

# Container-level security settings
# Drops all Linux capabilities and prevents privilege escalation
securityContext:
  allowPrivilegeEscalation: false
  capabilities:
    drop:
    - ALL
  runAsNonRoot: true
  runAsUser: 1000
  # readOnlyRootFilesystem: true  # Uncomment if your app doesn't need to write to disk

# ---------------------------------------------------------------------------
# SERVICE CONFIGURATION
# ---------------------------------------------------------------------------
# Kubernetes Service exposes the test pod within the cluster.
# The ingress controller routes external traffic to this service.
#
# Service Type Guide:
# - ClusterIP: Internal cluster access only (recommended when using ingress)
# - LoadBalancer: Creates cloud load balancer for direct external access
# - NodePort: Exposes on each node's IP at a static port (30000-32767)
service:
  type: ClusterIP  # Use ClusterIP when using ingress

  port: 80  # Port that the service listens on (ingress connects here)
  targetPort: 8080  # Port inside the container (FastAPI app listens here)

  # Only used if type: NodePort
  # nodePort: 30000  # Must be in range 30000-32767
  # Service accessible at: http://<any-node-ip>:30000

# ===============================================================================
# INGRESS CONFIGURATION
# ===============================================================================
# Configures how external traffic reaches the test pod.
# The ingress creates routes for the web UI and API endpoints.
#
# NOTE: This configures the ingress ROUTE for the test pod application.
#       For the NGINX Ingress Controller deployment, see: ingress-nginx (line 477)
#
ingress:
  enabled: true
  className: "nginx"
  annotations:
    # Disable SSL redirect (configure SSL at load balancer or reverse proxy level)
    nginx.ingress.kubernetes.io/ssl-redirect: "false"

    # File upload support - allows up to 50MB files for AI model testing
    nginx.ingress.kubernetes.io/proxy-body-size: "50m"
    nginx.ingress.kubernetes.io/client-body-buffer-size: "50m"

    # Timeouts for large file uploads - 5 minutes for complete upload
    nginx.ingress.kubernetes.io/proxy-connect-timeout: "300"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "300"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "300"

    # Request buffering for file uploads
    nginx.ingress.kubernetes.io/proxy-request-buffering: "on"
  hosts:
    # Replace with your actual domain
    - host: test.example.com
      paths:
        - path: /
          pathType: Prefix
  # Add additional hostnames if needed:
  # - host: validation.example.com
  #   paths:
  #     - path: /
  #       pathType: Prefix
  tls: []
    # Uncomment to enable TLS (requires cert-manager or manual certificate creation)
    # - secretName: airia-test-pod-tls
    #   hosts:
    #     - test.example.com

# ===============================================================================
# RESOURCE LIMITS
# ===============================================================================
resources:
  limits:
    cpu: 500m
    memory: 512Mi
    # Uncomment to allocate a GPU to the pod (shows GPU test in UI)
    # Requires GPU-enabled Kubernetes node with NVIDIA drivers
    # nvidia.com/gpu: 1
  requests:
    cpu: 100m
    memory: 256Mi
    # Uncomment to request GPU allocation
    # nvidia.com/gpu: 1

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 100
  targetCPUUtilizationPercentage: 80
  # targetMemoryUtilizationPercentage: 80

nodeSelector: {}

tolerations: []

affinity: {}

# ===============================================================================
# APPLICATION CONFIGURATION
# ===============================================================================
# Each enabled test appears as a card in the web UI dashboard.
# Configure credentials and endpoints for the services you want to validate.
#
# 🚀 QUICK START: Common Configuration Scenarios
#
# 1️⃣  Azure Infrastructure Testing:
#     Enable: postgresql, blobStorage
#
# 2️⃣  Azure + AI/ML Stack:
#     Enable: postgresql, blobStorage, openai, documentIntelligence
#
# 3️⃣  Self-hosted AI Stack:
#     Enable: postgresql, s3, openai (with baseUrl), or llama
#
# 4️⃣  Minimal Validation:
#     kubernetes test is always enabled automatically
#
# ===============================================================================
# TABLE OF CONTENTS
# ===============================================================================
#
# 📋 REQUIRED/CORE
#   - Authentication .......................... Line 192
#   - Kubernetes Storage (always enabled) ..... Line 424
#
# 🗄️ DATABASES
#   - PostgreSQL .............................. Line 207
#   - Apache Cassandra ........................ Line 223
#
# 💾 OBJECT STORAGE
#   - Azure Blob Storage ...................... Line 242
#   - MinIO (S3-compatible) ................... Line 254
#   - Amazon S3 ............................... Line 269
#
# 🤖 AI & MACHINE LEARNING
#   - Azure OpenAI & OpenAI-Compatible ........ Line 289
#   - Azure Document Intelligence ............. Line 329
#   - Dedicated Embedding Models .............. Line 345
#   - Self-hosted Llama Models ................ Line 372
#
# 🔧 INFRASTRUCTURE
#   - SSL Certificate Validation .............. Line 410
#   - Kubernetes Storage (always enabled) ..... Line 424
#   - GPU Detection ........................... Line 437
#
# ===============================================================================

config:
  # ╔═══════════════════════════════════════════════════════════════════════════╗
  # ║                        📋 REQUIRED/CORE CONFIGURATION                     ║
  # ╚═══════════════════════════════════════════════════════════════════════════╝

  # ---------------------------------------------------------------------------
  # AUTHENTICATION (Required)
  # ---------------------------------------------------------------------------
  # Credentials for accessing the web UI dashboard
  # Default: username=admin, password=changeme
  # IMPORTANT: Change these in production!
  auth:
    username: "admin"
    password: "changeme"
    secretKey: "your-super-secret-key-change-this"  # Used for JWT tokens

  # ╔═══════════════════════════════════════════════════════════════════════════╗
  # ║                            🗄️ DATABASE TESTS                              ║
  # ╚═══════════════════════════════════════════════════════════════════════════╝

  # ---------------------------------------------------------------------------
  # POSTGRESQL DATABASE TEST (Optional)
  # ---------------------------------------------------------------------------
  # UI Card: "PostgreSQL Database"
  # Tests: Connection, query execution, SSL/TLS, connection pooling
  # Set enabled: true to show this test in the UI
  postgresql:
    enabled: false
    host: "your-postgres-server.postgres.database.azure.com"
    port: "5432"
    database: "postgres"
    sslmode: "require"  # Options: disable, require, verify-ca, verify-full
    username: ""
    password: ""

  # ---------------------------------------------------------------------------
  # APACHE CASSANDRA TEST (Optional)
  # ---------------------------------------------------------------------------
  # UI Card: "Apache Cassandra"
  # Tests: Connection, keyspace access, read/write operations, multi-node
  # Set enabled: true to show this test in the UI
  cassandra:
    enabled: false
    hosts: ""  # Comma-separated list: host1,host2,host3
    port: "9042"
    username: ""
    password: ""
    keyspace: ""  # Optional: specific keyspace to test
    datacenter: "datacenter1"  # Optional: leave empty for single-DC
    useSsl: false

  # ╔═══════════════════════════════════════════════════════════════════════════╗
  # ║                          💾 OBJECT STORAGE TESTS                          ║
  # ╚═══════════════════════════════════════════════════════════════════════════╝

  # ---------------------------------------------------------------------------
  # AZURE BLOB STORAGE TEST (Optional)
  # ---------------------------------------------------------------------------
  # UI Card: "Azure Blob Storage"
  # Tests: Connection, container access, file upload/download, permissions
  # Set enabled: true to show this test in the UI
  blobStorage:
    enabled: false
    accountName: ""
    accountKey: ""
    containerName: "test-container"

  # ---------------------------------------------------------------------------
  # MINIO S3-COMPATIBLE STORAGE TEST (Optional)
  # ---------------------------------------------------------------------------
  # UI Card: "MinIO Storage"
  # Tests: Connection, bucket access, file upload/download/delete, encryption
  # Compatible with MinIO and other S3-compatible storage
  # Set enabled: true to show this test in the UI
  minio:
    enabled: false
    endpointUrl: "https://minio.example.com"
    accessKey: ""
    secretKey: ""
    bucketName: "test-bucket"
    secure: true  # Use HTTPS

  # ---------------------------------------------------------------------------
  # AMAZON S3 STORAGE TEST (Optional)
  # ---------------------------------------------------------------------------
  # UI Card: "Amazon S3 Storage"
  # Tests: Connection, bucket access, file upload/download/delete, encryption, versioning
  # Supports IAM roles, access keys, and temporary credentials
  # Set enabled: true to show this test in the UI
  s3:
    enabled: false
    region: "us-east-1"
    accessKeyId: ""
    secretAccessKey: ""
    sessionToken: ""  # Optional for temporary credentials (IAM roles)
    bucketName: "test-bucket"
    endpointUrl: ""  # Optional: for custom S3-compatible endpoints

  # ╔═══════════════════════════════════════════════════════════════════════════╗
  # ║                      🤖 AI & MACHINE LEARNING TESTS                       ║
  # ╚═══════════════════════════════════════════════════════════════════════════╝

  # ---------------------------------------------------------------------------
  # AZURE OPENAI & OPENAI-COMPATIBLE API TEST (Optional)
  # ---------------------------------------------------------------------------
  # UI Card: "Azure OpenAI & OpenAI-Compatible APIs"
  # Tests: Chat completion, embedding generation, API connectivity
  # Supports custom prompts and file uploads (PDFs, images) through UI
  #
  # This test supports TWO configuration modes:
  #
  # MODE 1: Azure OpenAI (Azure-hosted)
  # Use this for Microsoft Azure OpenAI Service
  openai:
    enabled: false
    endpoint: "https://your-openai.openai.azure.com/"
    apiKey: ""
    deploymentName: "gpt-35-turbo"  # Your Azure deployment name
    embeddingDeployment: ""  # Optional: Azure embedding deployment (e.g., "text-embedding-ada-002")
    # When embeddingDeployment is set, the test will also validate embedding generation

  # MODE 2: Self-hosted OpenAI-Compatible API
  # Use this for local LLMs (Ollama, vLLM, LocalAI, etc.) with OpenAI-compatible endpoints
  # Uncomment and configure these instead of the Azure settings above:
  # openai:
  #   enabled: true
  #   baseUrl: "http://your-llm-server:8080/v1"  # Your OpenAI-compatible endpoint
  #   apiKey: "your-api-key"  # May not be required for local endpoints
  #   modelName: "gpt-3.5-turbo"  # Model name to use
  #
  # Common self-hosted endpoints:
  # - Ollama: http://localhost:11434/v1
  # - vLLM: http://localhost:8000/v1
  # - LocalAI: http://localhost:8080/v1
  # - Text Generation Inference: http://localhost:8080/v1
  #
  # NOTE: For Llama-specific endpoints (non-OpenAI-compatible), use the 'llama' test below instead.

  # ---------------------------------------------------------------------------
  # AZURE DOCUMENT INTELLIGENCE TEST (Optional)
  # ---------------------------------------------------------------------------
  # UI Card: "Document Intelligence"
  # Tests: Document analysis, OCR, form recognition, table extraction
  # Supports PDF, image, and document file uploads (up to 25MB) through UI
  #
  # Use Case: Validate Azure Document Intelligence API for processing documents
  # - OCR text extraction from images and PDFs
  # - Table and form recognition
  # - Layout analysis and structure detection
  # - Page-by-page content analysis
  #
  # Set enabled: true to show this test in the UI
  documentIntelligence:
    enabled: false
    endpoint: "https://your-doc-intel.cognitiveservices.azure.com/"
    apiKey: ""
    model: "prebuilt-document"  # Options: prebuilt-document, prebuilt-invoice, prebuilt-receipt, etc.

  # ---------------------------------------------------------------------------
  # DEDICATED EMBEDDING MODEL TEST (Optional)
  # ---------------------------------------------------------------------------
  # UI Card: "Embedding Models"
  # Tests: Text vectorization, batch embedding generation, similarity calculations
  # Supports custom text input through UI for testing embeddings
  #
  # IMPORTANT: Only enable this if you have a SEPARATE embedding endpoint
  # (not Azure OpenAI). Azure OpenAI embeddings are tested within the 'openai' test above.
  #
  # Use Case: Standalone embedding services separate from your chat/completion API
  # Examples:
  # - OpenAI's embedding API (separate from Azure)
  # - Dedicated embedding servers (sentence-transformers, instructor-xl, etc.)
  # - Custom embedding endpoints
  #
  # Set enabled: true to show this test in the UI
  embeddings:
    enabled: false
    baseUrl: "https://api.openai.com/v1"  # OpenAI or custom embedding endpoint
    apiKey: ""
    modelName: "text-embedding-ada-002"  # Or: text-embedding-3-small, text-embedding-3-large
    customHeaders: ""  # Optional: Header1:Value1,Header2:Value2
    # Example for custom embedding server:
    # baseUrl: "http://embedding-server:8080/v1"
    # modelName: "sentence-transformers/all-MiniLM-L6-v2"

  # ---------------------------------------------------------------------------
  # OLLAMA NATIVE API TEST (Optional)
  # ---------------------------------------------------------------------------
  # UI Card: "Ollama API (Native)"
  # Tests: Native Ollama API endpoints (/api/generate, /api/chat, /api/tags)
  # Supports custom prompts and file uploads (PDFs, images) through UI
  #
  # Use Case: Test Ollama's native API (NOT OpenAI-compatible endpoints)
  # This test uses Ollama's native REST API endpoints for maximum compatibility
  # with Ollama-specific features.
  #
  # API Endpoints Tested:
  # - /api/version - Check Ollama version
  # - /api/tags - List available models
  # - /api/generate - Generate text completions
  # - /api/chat - Chat completions with message history
  #
  # IMPORTANT: For OpenAI-compatible Ollama endpoints (/v1/*), use the 'openai'
  # test above instead (set baseUrl to http://localhost:11434/v1).
  #
  # Set enabled: true to show this test in the UI
  ollama:
    enabled: false
    baseUrl: "http://localhost:11434"  # Ollama default endpoint
    modelName: "llama2"  # Or: llama3, llama3.1, codellama, mistral, phi, etc.
    maxTokens: 100  # Maximum tokens to generate
    temperature: 0.7  # Creativity (0.0-1.0): lower = more deterministic
    timeout: 60  # Request timeout in seconds
    #
    # Common Ollama Models:
    # - llama3.1:8b    - Meta Llama 3.1 (8 billion parameters)
    # - llama2         - Meta Llama 2 (default)
    # - codellama      - Code-specialized Llama model
    # - mistral        - Mistral 7B
    # - phi3           - Microsoft Phi-3
    # - gemma2         - Google Gemma 2
    #
    # To install models: ollama pull <model-name>
    # To list installed: ollama list

  # ╔═══════════════════════════════════════════════════════════════════════════╗
  # ║                         🔧 INFRASTRUCTURE TESTS                           ║
  # ╚═══════════════════════════════════════════════════════════════════════════╝

  # ---------------------------------------------------------------------------
  # SSL CERTIFICATE TEST (Optional)
  # ---------------------------------------------------------------------------
  # UI Card: "SSL Certificate Validation"
  # Tests: Certificate validity, expiration dates, chain validation, TLS versions
  # Monitors multiple URLs and warns when certificates expire soon
  # Set enabled: true to show this test in the UI
  ssl:
    enabled: false
    testUrls: "https://api.example.com,https://app.example.com"  # Comma-separated
    connectTimeout: 10  # Seconds
    warningDays: 30  # Warn if certificate expires within this many days

  # ---------------------------------------------------------------------------
  # KUBERNETES STORAGE TEST (Always Enabled)
  # ---------------------------------------------------------------------------
  # UI Card: "Kubernetes PVC Storage"
  # Tests: PVC creation, mounting, read/write operations, cleanup
  #
  # NOTE: This test is ALWAYS enabled and cannot be disabled.
  # It validates that Kubernetes can provision persistent storage in your cluster.
  kubernetes:
    storageClass: "default"  # StorageClass to use for test PVC
    testPvcSize: "1Gi"  # Size of test PVC

  # ---------------------------------------------------------------------------
  # GPU DETECTION TEST (Optional)
  # ---------------------------------------------------------------------------
  # UI Card: "GPU Detection"
  # Tests: NVIDIA GPU presence, driver version, CUDA availability, memory
  # Shows GPU information if available, or reports no GPU found
  # Set enabled: true to show this test in the UI
  # Note: Requires GPU node and nvidia.com/gpu resource allocation (see resources section)
  gpu:
    enabled: true
    required: false  # Set to true to FAIL the test if no GPU is found
    # ⚙️  RELATED SETTINGS:
    #     To allocate GPU to the pod, see: resources.limits.nvidia.com/gpu (line 118)

# ===============================================================================
# NAMESPACE CONFIGURATION
# ===============================================================================
namespace:
  create: false  # Set to true to create a new namespace
  name: "airia"  # Namespace name if create is true

# ===============================================================================
# VERSION CHECKING (Optional)
# ===============================================================================
# Helps ensure you're deploying the latest version of the chart
versionCheck:
  enabled: true  # Check for newer versions during helm upgrade
  useOCI: true  # Use OCI registry for version checks
  ociRepo: "oci://ghcr.io/davidpacold/airia-test-pod/charts"
  strict: false  # false = warn about newer versions, true = block old versions

# ===============================================================================
# NGINX INGRESS CONTROLLER CONFIGURATION
# ===============================================================================
# Automatically deploys NGINX Ingress Controller for routing traffic to the test pod.
# This is enabled by default for out-of-the-box functionality.
#
# NOTE: This configures the NGINX Ingress Controller deployment (the router itself).
#       For the test pod's ingress route configuration, see: ingress (line 80)
#
# IMPORTANT: Set enabled: false if you already have an ingress controller
# in your cluster to avoid conflicts and duplicate load balancers.
# ===============================================================================
ingress-nginx:
  enabled: true  # Set to false if you have an existing ingress controller
  controller:
    # Watch all ingresses in the cluster (not just those with ingressClassName)
    watchIngressWithoutClass: true
    # Watch ALL namespaces (not just the one where controller is deployed)
    scope:
      enabled: false
    # Make this the default ingress class in the cluster
    ingressClassResource:
      default: true
    # Service configuration - creates a LoadBalancer for external access
    service:
      type: LoadBalancer  # Creates cloud load balancer (NLB/ALB/etc)
      # Cloud provider annotations (uncomment as needed):
      #
      # -------------------------------------------------------------------------
      # AWS - Internet-facing Network Load Balancer (public access):
      # -------------------------------------------------------------------------
      # annotations:
      #   service.beta.kubernetes.io/aws-load-balancer-type: "nlb"
      #   service.beta.kubernetes.io/aws-load-balancer-scheme: "internet-facing"
      #   service.beta.kubernetes.io/aws-load-balancer-cross-zone-load-balancing-enabled: "true"
      #
      # -------------------------------------------------------------------------
      # AWS - Internal Network Load Balancer (private VPC access only):
      # -------------------------------------------------------------------------
      # annotations:
      #   service.beta.kubernetes.io/aws-load-balancer-type: "nlb"
      #   service.beta.kubernetes.io/aws-load-balancer-scheme: "internal"
      #   service.beta.kubernetes.io/aws-load-balancer-cross-zone-load-balancing-enabled: "true"
      #   # Optional: Specify subnets for internal LB
      #   # service.beta.kubernetes.io/aws-load-balancer-subnets: "subnet-xxxxx,subnet-yyyyy"
      #
      # -------------------------------------------------------------------------
      # Azure - Public Load Balancer (internet-facing):
      # -------------------------------------------------------------------------
      # annotations:
      #   service.beta.kubernetes.io/azure-load-balancer-internal: "false"
      #
      # -------------------------------------------------------------------------
      # Azure - Internal Load Balancer (private VNet access only):
      # -------------------------------------------------------------------------
      # annotations:
      #   service.beta.kubernetes.io/azure-load-balancer-internal: "true"
      #   # Optional: Specify subnet for internal LB
      #   # service.beta.kubernetes.io/azure-load-balancer-internal-subnet: "my-subnet"
      #
      # -------------------------------------------------------------------------
      # GCP - Public Load Balancer (internet-facing):
      # -------------------------------------------------------------------------
      # Default LoadBalancer type works out of the box for public access
      #
      # -------------------------------------------------------------------------
      # GCP - Internal Load Balancer (private VPC access only):
      # -------------------------------------------------------------------------
      # annotations:
      #   networking.gke.io/load-balancer-type: "Internal"
      #   # Optional: Specify subnet for internal LB
      #   # networking.gke.io/internal-load-balancer-subnet: "my-subnet"